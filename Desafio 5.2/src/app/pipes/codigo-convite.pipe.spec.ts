import { CodigoConvitePipe } from './codigo-convite.pipe';

describe('CodigoConvitePipe', () => {
  it('create an instance', () => {
    const pipe = new CodigoConvitePipe();
    expect(pipe).toBeTruthy();
  });
});
